# COMP30770 — Group Project Task List

| Task | Dependencies | Assignee | Notes |
|---|---|---|---|
| **Clean up code base** | — | Everyone | See if you have redundant files that made their way to `main`. Fix file names (e.g. `danny.ipynb`). Remove redundant folders (e.g. `results/`). Standardised naming: `spark_analysis`, `mysql_analysis`, `pandas_analysis`. |
| **Write-up** | Cleaning code base, `README.md` finalised, Measure memory | Everyone (then Adam + Danny do final report) | We should all add a short description to the README of the parts we worked on. Won't be permanent — just to help Adam and Danny understand the repo. |
| **`README.md` finalised** | Cleaning code base | — | — |
| **Get `spark_analysis` running and tested** | Measure memory | Simon (Windows), Josie (Mac) | Works on Adam's machine but he wrote it. |
| **What is `utils.py`?** | — | Liam | Thought to have been written to measure memory, but was used for measuring dataset creation. |
| **Measure memory for report** | Clean up code base, What is `utils.py` | Adam / Simon | Measure memory usage of `spark_analysis`, `sql_analysis`, `pandas_analysis` so we can compare. |
| **Do Danny's (`pandas_analysis`) and Josie's (`mysql_analysis`) use the same prototype dataset?** | — | Danny and Josie | Nice to have. Will simplify the report — avoids having to explain that different datasets were used for prototypes. |
| **Ask AI to grade our project** | All of the above | TBD | Add assignment brief files to the repo temporarily, then ask Claude Code / Cursor to grade the assignment. |